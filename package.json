{
  "name": "scheduler",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"npm run start --prefix client\" \"npm run server --prefix server\""
  },
  "author": "Chaersi",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
